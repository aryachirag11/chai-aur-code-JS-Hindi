   "use strcit"; // treat all JS code as newer version;

    //alert(3 - 1); // we are using nodeJS, not browser
     

// DATATYPES

let name = "Chiag Arya"; // string (use double quotes)
let age = 22; // number; // 2^52 is the range
let isAdult = true; // bool (true or false)
let salary = null; // standalone value
let gender; // undefined(value has not been defined)

// one more advanced datatype symbols => used for uniqueness
// objects

console.log(typeof name);
console.log(typeof age);
console.log(typeof isAdult);
console.log(typeof salary);
console.log(typeof gender);





